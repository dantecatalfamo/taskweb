<p style="color: green"><%= notice %></p>

<h1>Agenda</h1>

<ul id="dates" class="list-unstyled">
  <%#= render "agenda_today", day: @headings_by_date.first %>
  <%= render partial: "agenda_date", collection: @headings_by_date[0..], as: :day %>
</ul>

